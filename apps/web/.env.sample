# Supabase (Auth Google + dùng chung với API trên Vercel)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>

# Chỉ cần khi chạy local (production web + API cùng domain nên để trống)
VITE_API_URL=http://localhost:3000
